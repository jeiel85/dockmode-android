# Default proguard rules for DockMode release builds.
# Compose, AndroidX core, kotlinx Coroutines, and DataStore include their own
# keep rules via consumer-proguard configurations.

-keepattributes SourceFile,LineNumberTable
-renamesourcefileattribute SourceFile
