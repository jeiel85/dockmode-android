# SPEC.md

# DockMode 제품 설계서

```text
Project Code: DKM-ANDROID
Repository ID: dockmode-android
Android Application ID: io.jeiel85.dockmode
```

## 1. 제품 정의

DockMode는 Android 스마트폰을 충전 거치대에 올려두었을 때 사용할 수 있는 가로형 시계·달력 대시보드 앱입니다.

## 2. 문제 정의

사용자는 스마트폰을 책상, 침대 옆, 주방, 사무실 충전 스탠드에 올려두는 시간이 많습니다. 이때 잠금 화면만 보여주는 것보다 시간, 날짜, 오늘 일정, 다음 일정을 멀리서 보기 좋게 표시하면 실용성이 높습니다.

## 3. 목표

- 충전 거치 상태에서 보기 좋은 대형 시계 화면 제공
- 오늘 일정과 다음 일정을 빠르게 확인
- 야간에도 눈부심이 적은 저휘도 UI 제공
- Android 정책을 우회하지 않는 안정적인 자동화 경험 제공
- 네트워크 없이 동작 가능한 로컬 우선 앱 제공

## 4. 비목표

- Apple StandBy의 화면, 색상, 애니메이션, 구성 복제
- 충전 연결 즉시 백그라운드에서 Activity를 강제로 띄우는 동작
- Google Calendar REST API 로그인 연동
- 날씨 API, 광고, 분석 SDK, 원격 설정
- 잠금 화면 자체를 대체하는 기능

## 5. 주요 사용자 시나리오

### 시나리오 A: 수동 실행

1. 사용자가 DockMode 앱을 실행한다.
2. 앱이 현재 충전 상태와 화면 방향을 확인한다.
3. 충전 중이거나 사용자가 직접 대기 화면 시작을 누르면 `StandbyActivity`가 열린다.
4. 화면은 가로 전체화면으로 전환되고 시계, 날짜, 일정이 표시된다.

### 시나리오 B: 스크린세이버 모드

1. 사용자가 DockMode를 Android 스크린세이버로 설정한다.
2. 기기가 충전 중이고 유휴 상태가 된다.
3. Android 시스템이 `StandbyDreamService`를 실행한다.
4. DockMode가 시계·일정 대기 화면을 표시한다.

### 시나리오 C: 캘린더 권한 없음

1. 사용자가 일정 기능을 켜려 한다.
2. 앱이 캘린더 권한이 필요한 이유를 설명한다.
3. 사용자가 권한을 허용하면 오늘 일정이 표시된다.
4. 사용자가 거부하면 시계와 날짜만 표시하고, 일정 영역에는 권한 설정 안내를 표시한다.

## 6. 핵심 화면

### 6.1 HomeScreen

역할: 앱 진입, 대기 화면 시작, 권한 상태 확인, 스크린세이버 설정 안내.

구성:

- 앱 설명
- “대기 화면 시작” 버튼
- “캘린더 연동” 상태 카드
- “스크린세이버로 설정하기” 안내 카드
- 설정 진입 버튼

### 6.2 StandbyScreen

역할: 실제 가로형 대기 화면.

구성:

- 대형 현재 시간
- 날짜와 요일
- 다음 일정
- 오늘 일정 요약
- 충전 상태 아이콘 또는 텍스트
- 야간 모드 적용 상태

### 6.3 SettingsScreen

구성:

- 시계 스타일 선택
- 일정 표시 여부
- 야간 모드 자동 적용 여부
- 번인 방지 위치 이동 여부
- 화면 켜짐 유지 여부
- 개인정보/권한 안내

## 7. UI 원칙

- 가로모드에서 2m 거리에서도 시간이 읽혀야 한다.
- 일정 정보는 보조 정보로 두고, 시계 가독성을 해치지 않는다.
- 야간 모드는 검은색 또는 매우 어두운 배경, 낮은 대비의 텍스트를 사용한다.
- OLED 번인 방지를 위해 정적 요소는 장시간 같은 위치에 고정하지 않는다.
- 태블릿과 폴더블에서는 좌우 패널 구조를 사용한다.

## 8. 기능 요구사항

| ID | 요구사항 | 우선순위 | 완료 기준 |
|---|---|---:|---|
| FR-001 | 현재 시간을 초 단위 또는 분 단위로 갱신한다 | P0 | 화면의 시간이 실제 시스템 시간과 일치 |
| FR-002 | 날짜와 요일을 표시한다 | P0 | 로케일별 포맷 적용 |
| FR-003 | 충전 상태를 감지한다 | P0 | 충전/충전 아님/완충 상태 구분 |
| FR-004 | StandbyActivity에서 화면 꺼짐 방지를 적용한다 | P0 | Activity 표시 중 화면이 자동으로 꺼지지 않음 |
| FR-005 | Calendar Provider로 오늘 일정을 조회한다 | P1 | 권한 허용 후 오늘 일정 목록 표시 |
| FR-006 | 권한 거부 상태를 정상 처리한다 | P1 | 앱 크래시 없이 안내 문구 표시 |
| FR-007 | DreamService로 스크린세이버 모드를 제공한다 | P1 | Android 설정에서 선택 가능 |
| FR-008 | 번인 방지 위치 이동을 제공한다 | P2 | 설정 주기에 따라 주요 UI가 미세 이동 |
| FR-009 | 시계 스타일을 3종 제공한다 | P2 | 디지털, 미니멀, 캘린더 중심 모드 선택 가능 |

## 9. 비기능 요구사항

- 오프라인 기본 동작을 보장한다.
- 권한이 없어도 핵심 시계 기능은 동작한다.
- 대기 화면은 불필요한 애니메이션과 네트워크 호출을 하지 않는다.
- 장시간 표시 중 메모리 누수가 없어야 한다.
- 백그라운드 Activity 시작 제한을 우회하지 않는다.
- 배포 전 실제 기기에서 DreamService 동작을 확인한다.

## 10. 성공 기준

v1.0 기준 성공 기준:

- 사용자가 앱을 실행해 가로형 시계 화면을 볼 수 있다.
- 캘린더 권한을 허용하면 오늘 일정이 표시된다.
- Android 스크린세이버 설정에서 DockMode가 선택 가능하다.
- 충전 중 유휴 상태에서 DreamService 화면이 표시된다.
- GitHub Actions에서 Debug APK와 Release AAB가 생성된다.
- README, CHANGELOG, HISTORY, RELEASE 문서가 실제 구현과 일치한다.
